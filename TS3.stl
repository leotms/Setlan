program {
    using
        int i,j,k;
        bool b,c;
        set s;
    in
        i = 8;
        j = 12;
        k = 3;
        b = false; 
        s = {1, 2, 3}; 

        # Primer Print :) (este es valido)
        print i, j, k, b, s;

        # Segundo print
        print "Hello, world!";

        #Comprobamos! Primero se hace el parentesis :D
        k = i + (j + i);
        #a = i + j;
}
