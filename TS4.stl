program {
    using
        int i,j,k;
        bool b,c;
        set s;
    in {
        i = 8;
        j = 12;
        k = 3;
        b = false; 
        s = {1, 2, 3}; 
        
       { using
            int z, m, w;
            set c;
            bool k;
          in   

         scan z; 
       };

        # Primer Print :) (este es valido)
        print i, j, k, b, s;

        # Segundo print
        print "Hello, world!";

        scan s;
    }
}
